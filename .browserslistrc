# Modern browsers configuration
# This reduces JavaScript bundle size by targeting newer browsers

# Chrome >= 91 (supports most modern features)
Chrome >= 91

# Firefox >= 89 (supports most modern features)
Firefox >= 89

# Safari >= 14 (supports most modern features)
Safari >= 14

# Edge >= 91 (Chromium-based)
Edge >= 91

# iOS Safari >= 14
iOS >= 14

# Samsung Internet >= 14
Samsung >= 14

# Don't support dead browsers
not dead

# Don't support browsers with < 0.25% usage
not < 0.25%